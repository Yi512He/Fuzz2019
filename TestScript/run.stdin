TESTS=( t1 t2 t3 t4 t5 t6 t7 t8 t9 t10 t11 t12 )
NOTES=notes

echo Testing $* ... >> $NOTES

for test in ${TESTS[*]}
    # onintr InfiniteLoop
     do
       echo $1" <"$test">&" "/dev/null"
       #$1 <$test >& /dev/null
       $1 <$test>& /dev/null
       trap 'echo "time out"' 2
       if [ $? -eq 139 ]; then
          echo "core dumped"
       fi
     done
